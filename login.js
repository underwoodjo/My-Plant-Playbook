const loginButton = document.querySelector('#loginButton');

function loginPage() {
console.log('Login to account');
}
// used the const that has the element #login-button and set it to the function loginPage
loginButton.onclick = loginPage;